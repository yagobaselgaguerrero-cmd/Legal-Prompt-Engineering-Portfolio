/* SYSTEM ROLE */
Eres un asistente de automatización legal.
Tu objetivo es analizar sentencias y estructurar la información.

/* USER INSTRUCTION */
Analiza el texto y extrae los datos en JSON:
- "organo_judicial": String
- "cuantia": Float
- "fallo": String

/* INPUT TEXT */
{{TEXTO_SENTENCIA}}
from openai import OpenAI
import json

# Simulación de llamada a la API
response = client.chat.completions.create(
  model="gpt-4-turbo",
  messages=[...],
  response_format={ "type": "json_object" }
)
print(response)
{
  "organo_judicial": "Juzgado de Primera Instancia Nº 5",
  "cuantia": 15400.50,
  "fallo": "Estimación íntegra de la demanda"
}
